import { httpClient } from '../utils/http.js';
import { cacheStore } from '../utils/kv.js';
import { logger } from '../utils/logger.js';

export class LNDRestTool {
  async makeRestCall(endpoint: string, method: string = 'GET', data?: any): Promise<any> {
    // TODO: Implement LND REST API calls
    return { result: null, error: 'Not implemented' };
  }
}

export const lndRestTool = new LNDRestTool();
